// making a simple go "hello world" program

// First we define the package name
package main

// Then we import the fmt package for formatted I/O to do this add "fmt" to the import statement

// main function

func main() {

	// To print to console we are going to use the Println function from the fmt package to do this we type fmt.Println then add the string we want to add in parentheses and brackets like ("activity1")
	// Write the function below here

	// Finish the function above here
}

// Now lets run the program and test if we see "hello world" in the console

// Make sure you are in the activity 1 directory in your terminal, if not use CD to change in to it e.g cd activity1 and type the following command

// go run .
